[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "EazyML"
authors = [{name = "EazyML", email = "admin@eazyml.com"}]
dynamic = ["0.1.0", "description"]